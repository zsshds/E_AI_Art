## 1. 设置模型与默认值扩展

- [ ] 1.1 盘点现有图像相关设置键与默认值初始化逻辑，确定需要新增的接口配置项及其回退默认值
- [ ] 1.2 扩展后端设置更新白名单、类型定义和默认值初始化，覆盖文生图、编辑图、轮询、模型列表、聊天补全等接口配置

## 2. 后端接口解析与调用重构

- [ ] 2.1 在图像客户端中实现统一的接口地址解析逻辑，支持相对 Path 与完整 URL 两种输入
- [ ] 2.2 拆分文生图与编辑图的接口配置读取逻辑，并让轮询、模型列表、聊天补全分别读取对应配置
- [ ] 2.3 保持旧配置兼容与空值回退，确保未迁移完整配置时仍能工作或返回明确错误

## 3. 系统设置页面扩展

- [ ] 3.1 扩展前端设置类型定义与请求载荷，支持新增接口配置字段
- [ ] 3.2 更新系统设置页表单，增加分组后的接口配置输入框与说明文案
- [ ] 3.3 确认新增配置保存后能被“获取模型列表”和后续图像请求即时使用

## 4. 测试与回归验证

- [ ] 4.1 为图像客户端补充接口地址解析与回退行为测试
- [ ] 4.2 为设置处理器或相关 API 补充新增字段的保存/读取测试
- [ ] 4.3 验证典型第三方平台场景：生成接口与编辑接口不同、完整 URL 配置、相对 Path 配置